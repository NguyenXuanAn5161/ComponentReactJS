import { memo } from "react";

const HomePage = () => {
  return (
    <div>
      <h1 className="text-3xl font-bold text-red-400 text-center">HomePage</h1>
    </div>
  );
};

export default memo(HomePage);
